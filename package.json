{
  "name": "saga-factory",
  "version": "3.0.0",
  "description": "Minimizing boilerplate utility when using redux-saga",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets @babel/preset-env --out-dir lib",
    "prepublish": "npm run build"
  },
  "author": "Max Gram",
  "repository": {
    "type": "git",
    "url": "git://github.com/maxgram/saga-factory.git"
  },
  "bugs": {
    "url": "https://github.com/maxgram/saga-factory/issues"
  },
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.11.1",
    "@babel/preset-env": "^7.11.0"
  },
  "dependencies": {
    "redux-saga": "^1.1.3"
  }
}
